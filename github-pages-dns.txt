; ──────────────────────────────────────────────────────────────
;  wordshaping.org → GitHub Pages
;  BIND zone file for Cloudflare import
; ──────────────────────────────────────────────────────────────
;
;  Repo:  PlanetaryCouncil/wordshaping.org
;  Site:  planetarycouncil.github.io
;
;  ▸ UNCHECK "Proxy imported DNS records" before importing.
;    GitHub cannot complete its TLS certificate challenge through
;    Cloudflare's proxy. Import grey-cloud, let the certificate
;    issue, then turn proxying on afterwards if you want it.
;
;  ▸ Import these records FIRST. Only set the custom domain in
;    GitHub once they resolve — setting it early redirects the
;    working github.io URL to a domain that does not answer yet.
;
; ──────────────────────────────────────────────────────────────

$ORIGIN wordshaping.org.
$TTL 3600

; ── Apex → GitHub Pages, IPv4 ─────────────────────────────────
@       3600    IN      A       185.199.108.153
@       3600    IN      A       185.199.109.153
@       3600    IN      A       185.199.110.153
@       3600    IN      A       185.199.111.153

; ── Apex → GitHub Pages, IPv6 ─────────────────────────────────
@       3600    IN      AAAA    2606:50c0:8000::153
@       3600    IN      AAAA    2606:50c0:8001::153
@       3600    IN      AAAA    2606:50c0:8002::153
@       3600    IN      AAAA    2606:50c0:8003::153

; ── www → the org's Pages host ────────────────────────────────
;    Not the repo path. Trailing dot required.
www     3600    IN      CNAME   planetarycouncil.github.io.
